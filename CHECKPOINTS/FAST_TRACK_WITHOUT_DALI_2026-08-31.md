# Fast-track without DALI

Date: 2026-08-31
Branch: `mie/golden-forensic-v0.3`
State: `DALI_EXTERNAL_PENDING / DEVELOPMENT_CONTINUES`

DALI is optional and must not block the project. While authorization is pending, HookLab proceeds on two parallel paths:

1. Calibration path: RWC 2.0 / MedleyDB / Vocadito or other licensed paired representations are used only to validate representation stability. They never increase M300 N.
2. M300 evidence path: CoSoD/Harmonix and additional licensed providers increase scoped musical coverage with identity/version gates preserved.

Implemented fast-track components:
- `representation_calibration_feature_extractor.py`: provider-neutral melodic features.
- `paired_representation_agreement.py`: paired rho + median absolute error contract.
- existing `melody_representation_calibration_gate.py`: fail-closed promotion.

Frozen sequence:
`CALIBRATION CORPUS -> paired representation features -> agreement -> stable feature whitelist -> M300 association -> controls/multiplicity -> replication -> interpretation/theory -> conditioned deduction -> MIDI -> standardized audio -> exploratory then confirmatory listening`.

No DALI authorization is required to execute the calibration contract. DALI will be inserted later as an additional annotation provider when legitimately provisioned.

Current readiness remains engineering-heavy: the missing evidence is observed paired calibration data and a positive replicated M300 association. No synthetic association may be substituted to accelerate listening.

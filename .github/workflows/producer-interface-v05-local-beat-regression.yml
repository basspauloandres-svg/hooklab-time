name: Producer Interface v0.5 Local Beat Regression
on:
  push:
    branches: ["main"]
    paths:
      - "producer-interface-v0.5/**"
      - ".github/tests/producer-interface-v05-local-beat-regression.mjs"
      - ".github/workflows/producer-interface-v05-local-beat-regression.yml"
  workflow_dispatch:
jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - run: npm install --no-save playwright@1.55.0
      - run: npx playwright install chromium --with-deps
      - run: timeout 240s node .github/tests/producer-interface-v05-local-beat-regression.mjs

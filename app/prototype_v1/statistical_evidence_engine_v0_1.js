(function(g){'use strict';function finite(a,k){return a.map(x=>x.features&&+x.features[k]).filter(Number.isFinite)}function mean(a){return a.length?a.reduce((s,x)=>s+x,0)/a.length:null}function sd(a){if(a.length<2)return null;var m=mean(a);return Math.sqrt(a.reduce((s,x)=>s+(x-m)*(x-m),0)/(a.length-1))}function evaluate(store,opts){opts=opts||{};var cases=(store&&store.cases)||[],admitted=cases.filter(x=>x.scientific_state==='ADMITTED'),minN=Number.isFinite(opts.min_cases)?opts.min_cases:5,keys=opts.features||['pitch_range_semitones','interval_mean_abs','step_ratio','leap_ratio','repeat_ratio','duration_median_s','ioi_median_s','event_density_hz','metric_alignment_median_s'];var evidence={},eligible=[];keys.forEach(k=>{var vals=finite(admitted,k),rec={feature:k,n:vals.length,mean:mean(vals),sd:sd(vals),min:vals.length?Math.min(...vals):null,max:vals.length?Math.max(...vals):null,provenance_case_ids:admitted.filter(x=>Number.isFinite(+(x.features||{})[k])).map(x=>x.case_id),status:vals.length>=minN?'DESCRIPTIVE_EVIDENCE_AVAILABLE':'INSUFFICIENT_EVIDENCE'};evidence[k]=rec;if(rec.status==='DESCRIPTIVE_EVIDENCE_AVAILABLE')eligible.push(k)});return{schema:'HOOKLAB_STATISTICAL_EVIDENCE_v0.1',n_total:cases.length,n_admitted:admitted.length,min_cases_policy:minN,evidence:evidence,eligible_descriptive_features:eligible,generation_rules:[],scientific_d_unlocked:false,generation_state:'BLOCKED',reason:'Descriptive corpus statistics do not by themselves establish conditioned generative rules.'}}g.HookLabStatisticalEvidence={version:'0.1',evaluate:evaluate}})(window);